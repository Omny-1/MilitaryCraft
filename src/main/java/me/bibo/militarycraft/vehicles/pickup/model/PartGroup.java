package me.bibo.militarycraft.vehicles.pickup.model;

/**
 * Which sub-assembly a display part belongs to, and therefore what it rotates with: the hull, the
 * gun mount that turns with the gunner's aim, or the barrel that also pitches.
 */
public enum PartGroup {
    HULL,
    MOUNT,
    BARREL;

}

